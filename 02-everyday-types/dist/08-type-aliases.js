"use strict";
function printCoords(pt) {
}
printCoords({
    x: 100,
    y: 300
});
function printIds(id) {
}
printIds(100);
printIds('hello');
function userInput(str) {
    return str.slice(0.3);
}
userInput('hello');
